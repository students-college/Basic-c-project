#include <stdio.h>int main() {    char item[50];    float price;    int quantity;    printf("Enter item name: ");    scanf("%s", item);    printf("Enter price: ");    scanf("%f", &price);    printf("Enter quantity: ");    scanf("%d", &quantity);    float totalBill = price * quantity;    printf("Total Bill for %d %s(s): %.2f\n", quantity, item, totalBill);    return 0;}