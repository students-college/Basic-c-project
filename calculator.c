#include <stdio.h>int main() {    char op;    double num1, num2, result;    // ইউজারের কাছ থেকে অপারেটর ইনপুট নেওয়া    printf("Enter an operator (+, -, *, /): ");    scanf(" %c", &op);    // ইউজারের কাছ থেকে দুইটি সংখ্যা ইনপুট নেওয়া    printf("Enter two numbers: ");    scanf("%lf %lf", &num1, &num2);    // অপারেশন অনুযায়ী ফলাফল বের করা    switch(op) {        case '+':            result = num1 + num2;            break;        case '-':            result = num1 - num2;            break;        case '*':            result = num1 * num2;            break;        case '/':            if (num2 != 0)                result = num1 / num2;            else {                printf("Error! Division by zero.\n");                return 1;            }            break;        default:            printf("Invalid operator!\n");            return 1;    }    // ফলাফল প্রিন্ট করা    printf("Result: %.2lf\n", result);    return 0;}