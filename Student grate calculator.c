#include <stdio.h>int main() {    char name[50];    float marks;    // শিক্ষার্থীর নাম ইনপুট    printf("Enter student name: ");    scanf("%s", name);    // নম্বর ইনপুট    printf("Enter marks: ");    scanf("%f", &marks);    // গ্রেড নির্ধারণ    char grade;    if (marks >= 80)        grade = 'A';    else if (marks >= 70)        grade = 'B';    else if (marks >= 60)        grade = 'C';    else if (marks >= 50)        grade = 'D';    else        grade = 'F';    // আউটপুট    printf("Student: %s\nGrade: %c\n", name, grade);    return 0;}